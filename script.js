class Ball {
  constructor(x,y,w,h,vx,vy)
   this.h = h;
   this.w = w;
   this.x = x;
   this.y = y;
   this.vx = vx
   this.vy = vy
drawball() {
  createCanvas(400,400);
  backgroundcolour(0,250,32)
}
ball111(250,50,30,30.4,6)
}

drawball()